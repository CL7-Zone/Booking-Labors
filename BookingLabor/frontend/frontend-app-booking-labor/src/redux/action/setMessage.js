export const setMessage = (msg) => ({
    type: "SET_MESSAGE",
    message: msg,
});

export const setToken = (token) => ({
    type: "SET_TOKEN",
    token: token,
});













